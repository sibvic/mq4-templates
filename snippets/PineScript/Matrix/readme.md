# Matrix classes

## ISimpleTypeMatrix, ITableMatrix

`ISimpleTypeMatrix<CLASS_TYPE>` mirrors `ISimpleTypeArray<CLASS_TYPE>` for numeric matrices (e.g. `ISimpleTypeMatrix<double>` for float matrices).  
`ITableMatrix` — table matrices.

## FloatMatrix, TableMatrix

Matrix implementations
